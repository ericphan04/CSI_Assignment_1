// Solve the quadratic equation (ax2+bx+c=0) with a, b, c e integer and a<>0? (2 ma
#include<stdio.h>
int main(){
	int a, b, c;
	printf("Nhap a b c :"); scanf("%d %d %d",&a, &b, &c);
	 a*x^2+b*x+c=0;
	return 0;
}
